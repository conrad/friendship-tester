doctype html
html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(name='csrf-token', content=_csrf)
    title #{title} - Hackathon Starter
    != css('main')
  body

    .container
      include partials/flash

    h1(class="text-center") How well do you know your friends? 

    if !user
      p(class="text-center") This quiz tests if you can actually recognize people from your social media networks from their profile photos. Are you up for the challenge?
      a.btn.btn-block.btn-social(href='/auth/facebook')
        i.fa.fa-facebook
        | Sign in with Facebook
      a.btn.btn-block.btn-social(href='/auth/google')
        i.fa.fa-google-plus
        | Sign in with Google  
      h3(class="text-center") Your "friends" got the following scores:
      
    else
      block content
      a.btn.btn-block.btn-social(href='/logout')
        i.ion-log-out
        |  Logout  



    != js('application')
